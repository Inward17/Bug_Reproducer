You are a QA automation expert. Analyse the bug report below and return ONLY valid JSON.
No markdown. No prose. No code fences. Raw JSON only.

Bug report:
{bug_report}

Target URL:
{target_url}

CRITICAL: The goal is to REPRODUCE the bug — meaning PROVE it exists by triggering the buggy behavior.

Because bug reports are often ambiguous, you MUST generate 2-3 possible interpretations (variants)
of what the bug could be. Each variant should have its own success_condition.
The generated script will check ALL variants — if ANY one matches, the bug is considered reproduced.

CRITICAL: Every success_condition MUST be based on VISIBLE TEXT on the page.
Do NOT reference CSS selectors, element counts, URL paths, or structural assumptions.
The only reliable way to verify a bug is to check what TEXT appears on the page after performing the action.

Guidelines for "success_condition" in each variant:
- It must describe what VISIBLE TEXT you see on the page when the bug IS HAPPENING.
- ALWAYS phrase as: "Verify the visible body text contains '...' " or "Verify the visible body text does NOT contain '...'"
- Examples of CORRECT success_conditions:
  * "Login always fails" → "Verify the visible body text contains 'Invalid' or 'incorrect' or 'error' after submitting valid credentials"
  * "About page shows error" → "Verify the visible body text contains 'error' or 'failed to load' or 'could not' after clicking About tab"
  * "Form doesn't save data" → "Verify the visible body text does not contain 'saved' or 'success' after submitting form"
  * "Wrong price displayed" → "Verify the visible body text shows a price different from the expected value"
- WRONG: "Verify elements matching '.about-content' has length 0" (invented selector — might not exist even when page works)
- WRONG: "Verify URL contains '/about'" (SPAs don't change URL on tab navigation)
- WRONG: "Verify element is missing from DOM" (structural assumptions are unreliable)

Return this exact JSON structure:
{{
  "inferred_steps":    ["ordered list of steps a human would perform in a browser to trigger the bug"],
  "target_elements":   ["CSS selectors for key UI elements — use: input[name='...'], button[type='submit'], #id, .class"],
  "expected_behavior": "what SHOULD happen when the bug is NOT present",
  "success_condition": "visible text condition proving the bug IS present (primary interpretation)",
  "bug_variants": [
    {{
      "interpretation": "brief description of this possible interpretation of the bug",
      "success_condition": "visible text condition for this variant — MUST reference specific text to look for"
    }},
    {{
      "interpretation": "alternative interpretation of the bug",
      "success_condition": "visible text condition for this variant — MUST reference specific text to look for"
    }}
  ],
  "risk_factors":      ["potential issues: timing, auth, dynamic content, etc."]
}}